ADMIN_EMAIL=you@example.com
# Generate via: node -e "console.log(require('bcryptjs').hashSync('YOUR_PASSWORD', 10))"
ADMIN_PASSWORD_HASH=
# Generate via: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=
PORT=3000
PUBLIC_BASE_URL=http://localhost:3000
CORS_ORIGINS=http://localhost:3000,http://localhost:5180

# Browser origin for post-payment redirects (local Next dev is :3000).
PUBLIC_SITE_URL=http://localhost:3000

# Same UpSquad Razorpay merchant account as SquadBooks / Squad Payroll.
# Use LIVE keys in production. Webhook: POST /api/webhooks/razorpay
# event: payment_link.paid
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
